image: ubuntu:bionic

before_script:
  - apt-get update --yes
  - apt-get install --yes cmake libboost-dev libeigen3-dev

build:
  stage: build
  tags:
    - run1
  script:
    - mkdir build
    - cd build
    - cmake ..
    - cmake --build .
    - ctest -V
